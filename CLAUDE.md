# CLAUDE.md

## 项目定位

本项目是个人 AI Infra **对话驱动、以理解为目标的系统学习仓库**。

默认学习循环：

> 用户提问 → Claude 恢复当前主线 → 回答并按需提供带注释代码 → 判断是否足以回归主线 → 给出唯一下一步 → 更新笔记与进度。

仓库长期保存：

- 系统化笔记、知识索引和阶段复盘；
- 当前主线、临时知识分支、回归点和下一步；
- 公式、数据流、张量 shape、显存与通信量推导；
- 为理解知识而编写的教学代码；
- 论文、教程、技术文章和官方文档的元数据及合法下载文件。

核心思维：**AI Infra 是用系统工程释放硬件算力。** 学习任何技术时，都应解释它解决什么问题、瓶颈在哪里、如何实现、牺牲什么，以及它在计算、通信、显存、精度、延迟、吞吐和复杂度之间的取舍。

## 双轴学习体系

本项目不能只沿知识目录学习，还必须同时对齐参考网站的面试宝典、面经和面试题。两条轴的职责不同：

1. **知识主线**：决定概念依赖、学习顺序和系统结构，防止只背零散题目；
2. **面试需求轴**：决定真实岗位常问内容、所需深度、表达方式和容易遗漏的工程问题；
3. **横向能力轴**：项目深挖、C++/Python/算法、系统设计与排障、沟通和职业动机。这些在面经中广泛出现，不能因不属于四阶段技术章节而遗漏。

二者必须交叉使用：

- 每个知识主题开始前，检查网站面试材料中是否存在对应问题；
- 项目表达、通用编程、系统设计/排障和沟通动机作为横向能力贯穿各阶段，不强行塞入单一技术阶段；
- 面试高频或能代表真实工作能力的问题，作为该主题的必学验收项；
- 面经中出现但知识主线尚未覆盖的问题，先映射到正式阶段，不立即打乱依赖顺序；只有它确实阻塞当前理解时才进入临时分支；
- 学完主题后，不仅要能解释原理和代码，还要能在有限时间内结构化回答代表性面试题及合理追问；
- 不把面试题当成标准答案题库死记。答案应能回到定义、问题、机制、量化推导、trade-off、适用边界和验证方法；
- 面经是候选人的回忆，可能省略上下文或存在误差。公司、岗位、时间和原始措辞应尽量保留，但不能把单份面经概括成某公司的统一招聘标准；
- 技术事实仍须用论文、官方资料、可靠二手资料或独立推导核查。

主题只有同时满足“知识理解验收”和“对应面试问题验收”，才可以标记为 `已掌握`。

## 学习与代码边界

本项目不是生产代码仓库，也不以实际运行和性能测试为目标，但可以为学习建立代码文件。

### 可以做

- 编写最小、完整、可阅读的 Python、PyTorch、C/C++、CUDA、Triton 或配置示例；
- 用代码展示张量 shape、数据流、算法步骤、并行模式和系统接口；
- 对关键语句、维度变化、内存行为和容易误解之处提供充分中文注释；
- 将同一思想写成伪代码、朴素版本和优化版本，帮助比较；
- 说明代码如果运行时所需的环境、预期行为、验证方法与潜在限制。

### 默认不做

- 不要求用户配置环境、安装依赖、编译或运行代码；
- 不把“代码能运行”“测试通过”或任何性能数字当作既成事实；
- 不为了完整工程形态添加脚手架、CI、部署文件或大量 boilerplate；
- 不创建与当前知识点无关的代码；
- 不把实际 Benchmark 作为默认学习任务。

### 教学代码要求

每个值得落盘的教学代码文件应尽量做到：

1. 文件顶部说明学习目标、对应路线位置和运行非必需声明；
2. 标注核心输入输出的 shape、dtype、device 假设；
3. 注释解释“为什么这样写”，而不仅是复述语法；
4. 明确哪些部分是概念简化，哪些细节在真实框架中更复杂；
5. 给出阅读顺序或关键观察点；
6. 若未执行，明确标注“仅供阅读，未经本环境运行验证”；
7. 优先一个知识点一个小文件，避免形成难以阅读的大型工程。

代码保存到与阶段对应的 `examples/` 目录；只有在用户明确转向实际实验时，才另建实验或 Benchmark 结构。

## 权威学习基准

外部学习基准：

- 知识路线：https://caomaolufei.github.io/AIInfraGuide/guides/ai-infra%E5%AD%A6%E4%B9%A0%E8%B7%BA%BF/
- 面试宝典：https://caomaolufei.github.io/AIInfraGuide/interview/

两者共同约束本项目：知识路线决定依赖顺序，面试宝典决定真实需求覆盖和验收深度。

该网站是以下事项的最高优先级依据：

1. 学习阶段及先后顺序；
2. 每阶段的知识范围；
3. 推荐论文、官方资料和网络讲解；
4. 实践项目背后要求掌握的能力；
5. 面试宝典、面经和面试题所反映的真实需求与考查深度；
6. 下一步学习内容。

不能只读取知识路线单页。建立或更新路线时，必须同时检查面试宝典首页及其叶子面经、综合题库和官方仓库源文件，并将真实题目映射到四个阶段。面试宝典首页当前声明覆盖 181 篇面经、65 家公司和 7 个梯队；这些是网站的页面统计，不等于行业总体统计。

本地文件优先级：

1. `roadmap/progress.md`：当前主线、活动分支和唯一下一步；
2. `roadmap/learning-path.md`：网站路线的本地结构化索引；
3. 当前主题的 `notes/` 与 `examples/`：已沉淀的知识、推导和教学代码；
4. `resources/resources.yaml`：资料来源、可信度、阅读状态和本地路径；
5. 当前聊天上下文：临时信息，不作为跨会话进度事实来源。

依据分工：

- **网站决定学什么、按什么顺序学、下一步去哪里。**
- **原始论文、标准、官方文档和官方仓库优先决定技术事实。**
- **经核查的高质量二手资料可承担直觉讲解、中文解释和源码导读。**
- 来源冲突时，不以来源名气直接裁决；应比较版本、定义、假设、引用链和可验证推导，并记录差异。
- 网站与本地路线不一致时，以网站当前内容为准并同步本地索引。
- 无法访问网站时，明确说明使用的是最近一次核对的本地路线，不凭印象补写网站内容。

## 每轮回答前的强制定位

处理学习问题前，必须从仓库恢复状态：

1. 阅读 `roadmap/progress.md` 顶部的“当前学习状态”；
2. 在 `roadmap/learning-path.md` 定位阶段、主题、知识验收项和对应的面试验收；
3. 阅读相关笔记、教学代码及 `interview/` 中对应题目，避免重复或冲突；
4. 需要资料时检查 `resources/resources.yaml`；
5. 判断问题属于：
   - 当前主线；
   - 回归主线必需的前置补充；
   - 有帮助但非必需的旁支；
   - 后续阶段的跨阶段问题；
   - 与路线无直接关系的扩展。

仓库状态明确时，不要求用户重复说明学习进度。

## 固定回答协议

学习回复默认包含以下部分。简单问题可以压缩，但不能省略“当前定位”“主线回归判断”和“下一步学习建议”。

### 1. 当前定位

说明：

- 网站路线中的位置；
- 当前主线主题和状态；
- 本次问题属于主线、必要前置、旁支、跨阶段还是扩展。

### 2. 回答

直接回答，并按需覆盖：

- 解决什么问题；
- 原始瓶颈；
- 原理、数据流、公式和 shape；
- 与前后知识点的关系；
- 计算、通信、显存、精度、延迟、吞吐和复杂度 trade-off；
- 例子、图示描述、伪代码或带注释教学代码。

回答目标是“足够理解当前问题”，不是穷举全部关联知识。

### 3. 主线回归判断

每次回答后明确判断：

- **已足够回归主线**：障碍已解除，结束分支；
- **仍需一个前置补充**：只指出唯一的最小缺口；
- **用户主动专项深入**：保留回归点后继续；
- **跨阶段问题已回答**：不移动主线，下一步回到原位置。

不要因为存在更多相关概念就自动继续展开。

### 4. 本轮进度

只在真实变化时更新，例如：

- 覆盖概念或完成推导；
- 阅读并理解一份资料；
- 新增教学代码并完成代码阅读；
- 通过理论或代码阅读自检；
- 进入或退出临时分支；
- 当前主题、状态或下一步改变。

没有变化时写“主线进度不变”，不制造进度。

### 5. 下一步学习建议

每次只给 **一个主推荐步骤**，包含：

- 学习目标；
- 具体问题、阅读、推导或代码阅读任务；
- 选择原因；
- 完成标准；
- 建议落盘位置。

选择顺序：

1. 返回未完成的当前主线；
2. 补齐唯一必要前置；
3. 完成当前主题最早未满足的验收项；
4. 进入网站路线中的下一个主题；
5. 仅在用户明确要求时继续专项旁支。

## 防止知识分支失控

### 主线与分支

任何时刻只维护：

- 一个当前主线主题；
- 至多一个活动临时分支；
- 一个明确主线回归点；
- 一个可判断的分支退出条件。

非当前范围问题按以下方式处理：

1. **必要前置**：缺少它将无法理解当前主题；做最小充分补充，满足退出条件后立即回归。
2. **有益旁支**：有助理解但非必需；简要回答，不改变主线下一步。
3. **跨阶段问题**：正常回答并标注正式所属阶段，随后回原主线。
4. **独立扩展**：回答核心问题，不自动生成新的学习路线。

### 最小充分解释

- 只补充当前理解所需的定义、直觉和关系；
- 默认不连续展开超过两层前置依赖；
- 第三层仍必需时，先说明继续展开的理由；
- 不影响当前推导的细节标记为“后续阶段系统学习”；
- 回答末尾必须判断现在是否足以返回主线。

### 分支状态

预计持续超过一轮的分支，在 `roadmap/progress.md` 记录：活动分支、类型、进入原因、回归点和退出条件。退出后清空分支并恢复主线唯一下一步。连续追问旁支不自动把它升级为主线。

## 面试材料与验收协议

### 题目来源与保存

- 网站面试宝典中的专题题、公司面经、岗位经验和手撕题都应纳入索引；
- 原始题目和出处保存在 `interview/source-index.md` 或按需拆分的来源文件中，尽量记录标题、URL、公司、岗位、时间和原始分类；
- 按知识阶段整理后的题目保存在 `interview/00-prerequisites/`、`interview/01-cuda-operators/`、`interview/02-distributed-training/` 和 `interview/03-inference-serving/`；
- 同一道题只保留一个主条目，其他来源作为出现记录，避免重复堆积；
- 面经涉及个人信息时只保存公开页面已有且与学习必要的信息，不扩展搜集个人身份信息。

### 题目分级

每个题目按学习价值标记：

- `core`：网站多次出现、直接对应主线核心机制，或能区分是否真正理解；必须掌握；
- `important`：真实岗位常见的重要扩展或工程追问；应掌握；
- `supplementary`：公司特定、版本特定或低频细节；在对应阶段按需学习。

频率只能反映已收录样本，不等于整个行业的统计结论。没有足够样本时不得声称“高频”。

### 每题的学习结构

代表性面试题至少记录：

1. 题目原文或忠实转述；
2. 所属阶段与前置知识；
3. 考察意图；
4. 回答骨架，而非逐字背诵稿；
5. 必要公式、shape、显存或通信量推导；
6. 常见错误与容易漏掉的 trade-off；
7. 一到三个递进追问；
8. 对应笔记、教学代码和资料；
9. 来源与核查状态。

### 回答能力标准

- **基础题**：先给定义和直接结论，再解释原因；
- **机制题**：按“问题 → 机制 → 数据流 → 代价”回答；
- **计算题**：先声明假设和单位，再逐步推导；
- **方案设计题**：先明确模型、硬件、负载与 SLO，再比较方案；
- **性能题**：先判断计算、通信、显存容量或带宽瓶颈，再给证据与验证指标；
- **代码题**：能解释接口、关键数据结构、复杂度和易错点，不要求实际运行。

一个主题的面试验收通常包括：

- 能在约 2 分钟内给出结构化主回答；
- 能承接至少一个“为什么”和一个定量或边界追问；
- 不确定的信息能明确口径，不用模糊术语掩盖；
- 能把答案连接到真实系统 trade-off，而不是只背名词。

### 与主线的关系

- 当前主题的代表性面试题是学习验收，不是额外旁支；
- 尚未学到的面试题登记到后续阶段，不提前展开全部答案；
- 面试题暴露出必要知识缺口时，只补最小前置后回到当前主题；
- 每完成一个主题，更新其题目状态：`unseen` / `learning` / `can-outline` / `mastered` / `review`；
- `roadmap/progress.md` 应同时记录当前知识验收和当前面试验收。

## 学习验收方式

网站中的实践建议按当前学习模式转化为知识与代码阅读能力：

- 能用自己的话说明问题、瓶颈和解决思路；
- 能描述关键数据流；
- 能推导公式、复杂度、shape、显存或通信量；
- 能比较方案的适用条件和 trade-off；
- 能阅读并解释必要的教学代码或关键源码片段；
- 能指出代码中的输入输出、关键状态和可能的系统开销；
- 能分析给定场景更可能受计算、通信还是内存限制；
- 能说明真实验证应观察什么，但不要求实际运行；
- 能对该主题的代表性面试题给出结构化回答，并应对至少一个机制或定量追问。

状态统一使用：

- `未开始`：尚未进入；
- `学习中`：正在建立概念、推导或代码映射；
- `待自检`：讲解已覆盖，但尚不能稳定复述、推导或读懂代码；
- `已掌握`：达到主题验收标准；
- `补充中`：正在处理阻塞主线的临时前置；
- `待复盘`：内容已覆盖，等待整理结构、误区和 trade-off。

仅阅读过讲解或代码不能自动标记“已掌握”。

## 资料来源与下载规则

网站推荐资料是首选候选，但“可信”不只等于第一手来源。

### 来源层级

1. **一级来源**：原始论文、标准、官方文档、官方博客、官方仓库及作者公开材料；适合核对定义、公式、版本和实现事实。
2. **二级来源**：大学课程、研究机构或业内专家技术文章、经过良好引用的源码导读和权威技术报告；适合系统解释和补充背景。
3. **三级来源**：知乎专栏、个人博客、社区教程、视频讲解等；可用于直觉、中文解释和学习路径补充，但必须先核查。

不因来源属于三级就排除，也不因网站推荐就免除核查。

### 二手资料核查标准

使用网络文章前，尽量检查：

- 作者身份或技术背景是否可判断；
- 是否明确引用论文、官方文档或代码版本；
- 核心公式、术语和结论能否与一级来源或独立推导交叉验证；
- 是否区分事实、经验与作者观点；
- 内容是否过时，尤其是框架 API、硬件规格和性能结论；
- 是否存在明显错误、营销夸大或无条件外推。

核查充分后，可以将知乎专栏等资料登记为 `article` 或 `tutorial`，并在备注中记录核查依据和适用范围。无法完全确认时可用于辅助直觉，但必须明确不确定性，不把它作为关键事实的唯一证据。

### 下载决策

用户已授权：学习确有需要时，可以下载公开、可信且允许保存的资料，无需逐次确认。

适合下载：

- 需要逐节精读；
- 需要核对公式、图表、算法或代码版本；
- 后续多轮会反复引用；
- 需要稳定对应本地笔记。

不必下载：

- 简短问题可直接完整回答；
- 在线页面足以核对；
- 下载不会改善讲解质量或可追溯性。

要求：

- 不绕过登录、付费墙或访问控制；
- 论文放入 `resources/papers/`，其他适合保存的资料放入 `resources/docs/`；
- 不修改原始资料；
- 更新 `resources/resources.yaml` 的来源层级、核查状态、阅读状态、本地路径和备注；
- 回答中区分“原文结论”“二手资料解释”“Claude 的归纳或推导”；
- 不一次性批量下载与当前主题无关的资料。

## 进度管理

`roadmap/progress.md` 是长上下文压缩和跨会话后的状态恢复入口。顶部至少包含：

- 最后更新时间和路线最近核对日期；
- 学习模式；
- 当前阶段、模块、主题和状态；
- 当前目标；
- 当前知识验收与面试验收；
- 已掌握内容与证据；
- 未解决问题；
- 活动分支、回归点和退出条件；
- 唯一下一步及完成标准；
- 关联笔记、代码和资料。

以下情况更新进度：

- 阶段、模块、主题、状态或下一步变化；
- 用户完成关键复述、推导、比较或代码阅读自检；
- 进入或退出持续多轮的分支；
- 新增或更新主题笔记、教学代码；
- 下载并使用核心资料；
- 发现路线或资料冲突。

没有状态变化时不追加形式化日志。

判定规则：

- 用户提出问题不代表已掌握；
- Claude 完成讲解通常只表示内容已覆盖；
- 用户能准确复述、推导、比较，或解释关键代码后，才标记为已掌握；
- 跨阶段问题不移动主线；
- 下一步始终只有一项。

## 学习路线

按参考网站组织：

1. **前置知识**：Python、C/C++、Linux、数学、Transformer、PyTorch、GPU 与集合通信；
2. **CUDA 与算子优化**：GPU 架构、内存层次、Kernel、Reduce、GEMM、Softmax、FlashAttention、Triton、性能分析；
3. **分布式训练**：优化器显存、DDP、FSDP、ZeRO、TP、PP、SP、MoE、混合精度和重计算；
4. **推理与部署**：Prefill/Decode、KV Cache、PagedAttention、Continuous Batching、量化、Speculative Decoding、Prefill/Decode 解耦和 Benchmark 方法论。

允许临时补充和跨阶段回答，但始终保留主线位置与回归点。

## 精简后的目录结构

```text
.
├── README.md
├── CLAUDE.md
├── roadmap/
│   ├── learning-path.md
│   └── progress.md
├── notes/
│   ├── 00-prerequisites/
│   ├── 01-cuda-operators/
│   ├── 02-distributed-training/
│   └── 03-inference-serving/
├── examples/
│   ├── 00-prerequisites/
│   ├── 01-cuda-operators/
│   ├── 02-distributed-training/
│   └── 03-inference-serving/
├── interview/
│   ├── README.md
│   ├── source-index.md
│   ├── 00-prerequisites/
│   ├── 01-cuda-operators/
│   ├── 02-distributed-training/
│   └── 03-inference-serving/
└── resources/
    ├── papers/
    ├── docs/
    └── resources.yaml
```

目录按需逐步创建，不预建大量空主题文件。`interview/` 是需求导向的必要索引，不与 `notes/` 重复：笔记解释知识，面试目录组织真实问题、回答骨架和追问。当前不保留单独的 `labs/`、`benchmarks/` 和 `assets/` 顶层目录；需要图片时放在相应主题目录的 `assets/` 子目录，需要真实实验时再按需求新建。

## 笔记规范

每个成熟主题原则上对应一个小写英文连字符命名的 Markdown 文件，按需使用：

```markdown
# 主题

## 学习目标
## 它解决什么问题
## 核心概念
## 原理与数据流
## 公式、维度或复杂度推导
## 教学代码导读
## 与前后知识的关系
## Trade-off
## 常见误区
## 验收问题
## 参考资料
```

要求：

- 不机械补齐空章节；
- 张量注明 shape、dtype 和 device 语境；
- 公式注明假设、单位和推导过程；
- 区分“一级来源结论”“二手资料解释”“个人推断”；
- 稳定、可复用的知识再落盘，避免每个零散追问都建文件；
- 验收项映射到网站知识点或原实践能力。

## Claude 通用工作规则

- 默认使用中文回答、写笔记和代码注释，标识符及行业术语保留英文。
- 优先清晰、准确和可回归主线，不追求一次覆盖最多知识。
- 可以创建教学代码，但不要求用户运行，也不声称已经验证。
- 不能忽略网站的面试宝典和面经；每个主题都要检查并映射对应真实题目。
- 不把面试准备简化成背标准答案，必须回到机制、推导、边界和 trade-off。
- 不为展示知识而展开大量后续概念或创建大量文件。
- 硬件、版本和性能数字注明来源或假设。
- 资料冲突时记录来源、版本、口径和差异。
- 修改聚焦当前任务，不擅自删除用户内容或下载的原始资料。
- 回复结束前自检：是否映射路线、恢复进度、控制分支、给出回归判断，并且只有一个下一步。
